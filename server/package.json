{
  "name": "pcsim-vault-server",
  "version": "2.0.0",
  "private": true,
  "description": "Сервер PCSIM Vault (MongoDB + Mongoose): раздаёт сайт и хранит данные в MongoDB",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "mongoose": "^8.24.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
